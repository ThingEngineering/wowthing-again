@model ErrorViewModel
@{
    ViewData["Title"] = "Not Found";
}

<div class="content">
    <div class="error">
        <h1>404 Not Found</h1>
        <p>The provided path does not exist</p>

        @if (Model.ShowRequestId)
        {
            <p>
                ID: <code>@Model.RequestId</code>
            </p>
        }
    </div>
</div>
