// Enum.TooltipDataItemBinding
export enum ItemBinding {
    Account = 1, // Warbound
    BnetAccount = 2, // ??
    Soulbound = 3,
    BindToAccount = 4, // ??
    BindToBnetAccount = 5, // Binds to Warband
    BindOnPickup = 6,
    BindOnEquip = 7,
    BindOnUse = 8,
    AccountUntilEquipped = 9, // Warbound until equipped
    BindToAccountUntilEquipped = 10, // Binds to Warband until equipped
}
